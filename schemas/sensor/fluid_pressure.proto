syntax = "proto3";

package vmsgs.proto.sensor;

import "common/header.proto";

message FluidPressure {
  vmsgs.proto.common.Header header = 1;
  double fluid_pressure = 2;  // Pa
  double variance = 3;        // 0 if unknown
}
