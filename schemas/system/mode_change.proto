syntax = "proto3";

package vmsgs.proto.system;

import "common/header.proto";
import "common/status.proto";

// Generic mode-change RPC schema reused by operation_mode / control_mode / engage.
message ModeChangeRequest {
  // Target mode token; semantics per service.
  uint32 target = 1;

  // Operator- or controller-supplied reason for audit logs; may be empty.
  string reason = 2;
}

// Acknowledgement returned after evaluating or applying a mode-change request.
message ModeChangeResponse {
  // Time when the response outcome was produced.
  vmsgs.proto.common.Header header = 1;

  // Canonical success or failure result with optional detail.
  vmsgs.proto.common.Status status = 2;
}
